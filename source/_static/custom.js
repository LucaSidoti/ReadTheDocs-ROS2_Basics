document.addEventListener("DOMContentLoaded", function () {
    const blocks = document.querySelectorAll(".admonition");
    blocks.forEach((block) => {
        const title = block.querySelector(".admonition-title");
        if (title && title.innerText.trim() === "Question") {
            // Apply styles to the entire Question block
            block.style.backgroundColor = "#e6ccff"; // Pastel purple background for the block
            block.style.borderLeft = "0px solid #e6ccff"; // Pastel purple border for emphasis

            // Apply styles to the title bar of the Question block
            title.style.backgroundColor = "#9933ff"; // Dark purple for the title bar
            title.style.color = "#ffffff"; // White text for the title
            title.style.fontWeight = "bold"; // Make title text bold
            title.style.padding = "5px 10px"; // Add padding inside the title
            title.style.borderBottom = "0px solid #e6ccff"; // Subtle separator below the title
        }
    });
});


document.addEventListener("DOMContentLoaded", function () {
    const blocks = document.querySelectorAll(".admonition");
    blocks.forEach((block) => {
        const title = block.querySelector(".admonition-title");
        if (title && title.innerText.trim().startsWith("Thymio")) {
            // Apply styles to the entire Thymio block
            block.style.backgroundColor = "#ddefff"; // Soft dark blue background for the block
            block.style.borderLeft = "0px solid #99ccff"; // Light blue border for emphasis

            // Apply styles to the title bar of the Thymio block
            title.style.backgroundColor = "#003366"; // Dark blue for the title bar
            title.style.color = "#ffffff"; // White text for the title
            title.style.fontWeight = "bold"; // Make title text bold
            title.style.padding = "5px 10px"; // Add padding inside the title
            title.style.borderBottom = "0px solid #99ccff"; // Subtle separator below the title
        }
    });
});

document.addEventListener("DOMContentLoaded", function () {
    const blocks = document.querySelectorAll(".admonition");
    blocks.forEach((block) => {
        const title = block.querySelector(".admonition-title");
        if (title && title.innerText.trim() === "Hints") {
            // Apply styles to the entire Hints block
            block.style.backgroundColor = "#e0f4ff"; // Lighter blue background for the block
            block.style.borderLeft = "0px solid #66a3ff"; // Soft blue border for emphasis

            // Apply styles to the title bar of the Hints block
            title.style.backgroundColor = "#005b99"; // Vibrant blue for the title bar
            title.style.color = "#ffffff"; // White text for the title
            title.style.fontWeight = "bold"; // Make title text bold
            title.style.padding = "5px 10px"; // Add padding inside the title
            title.style.borderBottom = "0px solid #66a3ff"; // Subtle separator below the title
        }
    });
});


document.addEventListener("DOMContentLoaded", function () {
    const blocks = document.querySelectorAll(".admonition");
    blocks.forEach((block) => {
        const title = block.querySelector(".admonition-title");
        if (title && title.innerText.trim().startsWith("Task")) {
            // Apply styles to the entire Thymio block
            block.style.backgroundColor = "#ddefff"; // Soft dark blue background for the block
            block.style.borderLeft = "0px solid #99ccff"; // Light blue border for emphasis

            // Apply styles to the title bar of the Thymio block
            title.style.backgroundColor = "#003366"; // Dark blue for the title bar
            title.style.color = "#ffffff"; // White text for the title
            title.style.fontWeight = "bold"; // Make title text bold
            title.style.padding = "5px 10px"; // Add padding inside the title
            title.style.borderBottom = "0px solid #99ccff"; // Subtle separator below the title
        }
    });
});


document.addEventListener("DOMContentLoaded", function () {
    const blocks = document.querySelectorAll(".admonition");
    blocks.forEach((block) => {
        const title = block.querySelector(".admonition-title");
        if (title && title.innerText.trim().startsWith("Action Required")) {
            // Apply styles to the entire "Do Not Miss It" block
            block.style.backgroundColor = "#ffe4e6"; // Soft pastel pink background
            block.style.borderLeft = "0px solid #ff99aa"; // Light pink border for emphasis

            // Apply styles to the title bar of the "Do Not Miss It" block
            title.style.backgroundColor = "#ff99aa"; // Slightly darker pastel pink for the title bar
            title.style.color = "#ffffff"; // White text for the title
            title.style.fontWeight = "bold"; // Make the title text bold
            title.style.padding = "5px 10px"; // Add padding to the title
            title.style.borderBottom = "0px solid #ff6677"; // Separator below the title
       
        }
    });
});


document.addEventListener("DOMContentLoaded", function () {
    const blocks = document.querySelectorAll(".admonition");
    blocks.forEach((block) => {
        const title = block.querySelector(".admonition-title");
        if (title && title.innerText.trim().startsWith("Procedure")) {
            // Apply styles to the entire "Procedure" block
            block.style.backgroundColor = "#e3f2fd"; // Light pastel blue background
            block.style.borderLeft = "0px solid #64b5f6"; // Medium blue border

            // Apply styles to the title bar of the "Procedure" block
            title.style.backgroundColor = "#00acc1"; // Rich turquoise for the title background
            title.style.color = "#ffffff"; // White text for contrast
            title.style.fontWeight = "bold"; // Make the title text bold
            title.style.padding = "5px 10px"; // Add padding to the title
            title.style.borderBottom = "0px solid #0d47a1"; // Subtle darker separator
        }
    });
});


document.addEventListener("DOMContentLoaded", function () {
    const blocks = document.querySelectorAll(".admonition");
    blocks.forEach((block) => {
        const title = block.querySelector(".admonition-title");
        if (title && title.innerText.trim().startsWith("Steps")) {
            // Apply styles to the entire Thymio block
            block.style.backgroundColor = "#ddefff"; // Soft dark blue background for the block
            block.style.borderLeft = "0px solid #99ccff"; // Light blue border for emphasis

            // Apply styles to the title bar of the Thymio block
            title.style.backgroundColor = "#003366"; // Dark blue for the title bar
            title.style.color = "#ffffff"; // White text for the title
            title.style.fontWeight = "bold"; // Make title text bold
            title.style.padding = "5px 10px"; // Add padding inside the title
            title.style.borderBottom = "0px solid #99ccff"; // Subtle separator below the title
        }
    });
});


document.addEventListener("DOMContentLoaded", function () {
    const blocks = document.querySelectorAll(".admonition");
    blocks.forEach((block) => {
        const title = block.querySelector(".admonition-title");
        if (title && title.innerText.trim().startsWith("Useful")) {
            // Apply styles to the entire Thymio block
            block.style.backgroundColor = "#ddefff"; // Soft dark blue background for the block
            block.style.borderLeft = "0px solid #99ccff"; // Light blue border for emphasis

            // Apply styles to the title bar of the Thymio block
            title.style.backgroundColor = "#003366"; // Dark blue for the title bar
            title.style.color = "#ffffff"; // White text for the title
            title.style.fontWeight = "bold"; // Make title text bold
            title.style.padding = "5px 10px"; // Add padding inside the title
            title.style.borderBottom = "0px solid #99ccff"; // Subtle separator below the title
        }
    });
});


document.addEventListener("DOMContentLoaded", function () {
    const blocks = document.querySelectorAll(".admonition");
    blocks.forEach((block) => {
        const title = block.querySelector(".admonition-title");
        if (title && title.innerText.trim().startsWith("Minimal")) {
            // Apply styles to the entire Hints block
            block.style.backgroundColor = "#e0f4ff"; // Lighter blue background for the block
            block.style.borderLeft = "0px solid #66a3ff"; // Soft blue border for emphasis

            // Apply styles to the title bar of the Hints block
            title.style.backgroundColor = "#005b99"; // Vibrant blue for the title bar
            title.style.color = "#ffffff"; // White text for the title
            title.style.fontWeight = "bold"; // Make title text bold
            title.style.padding = "5px 10px"; // Add padding inside the title
            title.style.borderBottom = "0px solid #66a3ff"; // Subtle separator below the title
        }
    });
});


document.addEventListener("DOMContentLoaded", function () {
    const blocks = document.querySelectorAll(".admonition");
    blocks.forEach((block) => {
        const title = block.querySelector(".admonition-title");
        if (title && title.innerText.trim().startsWith("Test")) {
            // Apply styles to the entire Test block
            block.style.backgroundColor = "#fff4e0"; // Lighter orange background for the block
            block.style.borderLeft = "0px solid #ffa500"; // Orange border for emphasis

            // Apply styles to the title bar of the Test block
            title.style.backgroundColor = "#ffb380"; // Pastel orange for the title bar
            title.style.color = "#ffffff"; // White text for the title
            title.style.fontWeight = "bold"; // Make title text bold
            title.style.padding = "5px 10px"; // Add padding inside the title
            title.style.borderBottom = "0px solid #ffa500"; // Subtle separator below the title
        }
    });
});
